name: CI - Validation

on:
  push:
    paths-ignore: 
      - 'README.md'
      - 'CHANGELOG.md'
      - 'LICENSE'
      - 'docs/**'
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build:
    name: Validate
    runs-on: macos-15-intel
    if: github.repository_owner == 'dortania'
    steps:
      - uses: actions/checkout@v4
      - name: Install package requirements
        run: /Library/Frameworks/Python.framework/Versions/3.11/bin/pip3 install -r requirements.txt
      - name: Validate
        run: /Library/Frameworks/Python.framework/Versions/3.11/bin/python3 Skyfall-GUI.command --validate